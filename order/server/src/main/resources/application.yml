spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/spring_cloud_sell?characterEncoding=utf-8&useSSL=false
    username: root
    password: justdoit111757
  jpa:
    show-sql: true
  main:
    allow-bean-definition-overriding: true    # 允许重复定义多个Feign客户端实例
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
PRODUCT:
  ribbon:
    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule    # Ribbon 负载均衡策略（配置key可以从Spring官网查询，value从IRule的实现类选择）